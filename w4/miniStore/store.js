const users = []
const productsIC = {} // id and count //new Map()

function addProduct(product, count) {
                                        // for (const [productId, productCount] of productsIC) {
                                        //     if (product.id === productId) {
                                        //         productCount += count
                                        //         return
                                        //     }
                                        // }
                                        // productsIC.set(product.id, count)
    if (product in productId) {
       productCount += count;
       return
    }else{
        productsIC ?? //.set(product.id, count)
    }
}

function removeProduct(product, count) {
    
}



